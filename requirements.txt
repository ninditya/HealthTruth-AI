# Core dependencies
streamlit
python-dotenv
numpy
faiss-cpu
Pillow
urllib3<2
PyPDF2

# OpenRouter (OpenAI-compatible API)
openai>=1.0.0

# Local embeddings (no API needed)
sentence-transformers>=2.2.0

# Vector store backends (faiss = local/dev, qdrant = staging/production)
qdrant-client>=1.9.0

# LangChain versi stabil untuk Python 3.9
langchain==0.3.0
langchain-core==0.3.0
langchain-community==0.3.0
